-- GENERATED by platform_ops.simulation.codegen. Do not edit by hand:
-- change the spec in that module and rerun it.
select
    cast(date_trunc('week', started_at) as date) as period_start,
    landing_page,
    count(*) as sessions,
    count(*) filter (where converted) as converted_sessions,
    sum(pageviews) as pageviews
from {{ ref('marketing_fct_sessions') }}
group by 1, 2
