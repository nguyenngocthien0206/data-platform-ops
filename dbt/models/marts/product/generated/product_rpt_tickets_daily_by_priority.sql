-- GENERATED by platform_ops.simulation.codegen. Do not edit by hand:
-- change the spec in that module and rerun it.
select
    cast(date_trunc('day', created_at) as date) as period_start,
    priority,
    count(*) as tickets,
    count(*) filter (where breached_sla) as breached_tickets,
    round(avg(resolution_hours), 2) as avg_resolution_hours
from {{ ref('product_fct_support_tickets') }}
group by 1, 2
