-- GENERATED by platform_ops.simulation.codegen. Do not edit by hand:
-- change the spec in that module and rerun it.
select
    cast(date_trunc('month', ordered_at) as date) as period_start,
    category,
    sum(quantity) as units,
    sum(line_amount) as revenue,
    sum(line_margin) as margin
from {{ ref('sales_fct_order_items') }}
group by 1, 2
