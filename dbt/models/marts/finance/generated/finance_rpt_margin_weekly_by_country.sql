-- GENERATED by platform_ops.simulation.codegen. Do not edit by hand:
-- change the spec in that module and rerun it.
select
    cast(date_trunc('week', ordered_at) as date) as period_start,
    country,
    sum(net_amount) as net_revenue,
    sum(cost_amount) as cost,
    sum(margin_amount) as margin
from {{ ref('finance_fct_gross_margin') }}
group by 1, 2
